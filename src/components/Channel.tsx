import React from 'react';
import MultiInput from './MultiInput';

export default function Channel({ label }: any) {
  return (
    <div className='sidebar-item'>
      <p>{label}</p>
    </div>
  );
}
